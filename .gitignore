node_modules/
dist/
shots/
.DS_Store
*.log
.claude/agent-memory/
.claude/worktrees/
scratch/

# Terraform: state contains resource metadata and must never be committed; tfvars may
# contain account-specific values. The lock file IS committed, deliberately.
infra/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
