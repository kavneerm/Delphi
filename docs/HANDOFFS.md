# HANDOFFS (append-only)
Format: `<timestamp> <from> → <to>: <what merged, commit, how to use>`
2026-09-05 agent0-contracts → all agents: contracts_v1 on branch agent0-contracts (7e00826) — spec_schema, action_schema (root = decision output, x-action-ladder = the 14-rung menu), event_log_schema, inject_schema, lake_record_schema, s3_layout.md, README.md, examples/. Schemas $ref each other by bare filename: build a referencing Registry, recipe in contracts/README.md or tests/agent0-contracts/test_contracts.py. Open questions in contracts/QUESTIONS.md.
2026-09-05 agent0-contracts → all agents: NINE-SEAT FOLD, same branch. Seats are now northcom, usspacecom, nsc, norway, northern_fleet, kremlin, china, starlink, iridium. nato and ksat folded into utility terms and Norway's ground segment; hacktivist folded into the hacktivist_injects rule actor (per-run hidden affiliation). nsc is the human-playable seat. New contract contracts/env_config_schema.json: clock_mode {continuous | checkpoint(fixed|variable_tempo|adaptive)} and release_policy {human | auto(approval_probability)}. event_log gains checkpoint, release_requested, release_granted, release_denied, human_action. Both modes emit the same log — do not branch on it. seats.md rewritten with Design notes. See contracts/REPORT.md and QUESTIONS.md Q9/Q10.
2026-09-05 agent1-engine → agent7-ui: engine/samples/stub_run.jsonl on branch agent1-engine (c9809b4) — a real 72-hour episode log, 3473 lines, every line valid against event_log_schema, ordered, ends in episode_end. Stop generating stub logs from the schema and play this one back. engine/samples/README.md says where ground tracks (state_change what="assets.ground_tracks", every 30 sim min), the storm overlay (storm_update), the ladder (action lines + x-action-ladder rung order), the release cycle (release_requested/granted/denied joined on payload.release_id) and the end-of-episode ground-truth reveal live. Note: beliefs and reasoning are lake-record fields, not event-log fields — ask if you need them on the persona cards and the engine will add them to the action payload.
2026-09-05 agent1-engine → agent2-calib: the engine reads calib/ but does not require it. engine/storm.py reads calib/storm_effects.csv columns profile, severity, peak_kp, min_dst_nt, onset_hours, plateau_hours, decay_hours, safe_mode_heo_node_per_hour, safe_mode_constellation_per_member_hour, safe_mode_pass_sensor_per_hour, tracking_degradation_hours, screening_suspension_hours, satellites_lost, source_url — all optional, each missing one falls back to a placeholder tagged TODO_CALIB — plus an optional per-profile series calib/kp_<profile>.csv with time_s (or hours), kp, dst_nt. engine/attacks.py reads calib/attribution_lags.csv columns attack_type (jam|dazzle|ground_cyber|rpo or the counter_rpo rung name), distribution, median_hours, sigma, floor_hours, source_url. Add columns freely; the loader ignores what it does not know.
2026-09-05 agent1-engine → coordinator/human: HAZARD, not a handoff. The twelve agents are sharing one git checkout and one HEAD rather than a worktree each, so a branch switch by one agent moves everyone. Two of my commits (4b36e68, 8acabb8) landed on whichever branch happened to be current and are now reachable from agent4-train and agent10-replays; their content is engine/ only and is rebuilt correctly on agent1-engine as 2bb8372. Those two branches should drop or ignore the engine/ files in their history before opening a PR. I have moved to a dedicated worktree at ../Panoptes-agent1-engine; other agents may want to do the same.
2026-09-05 agent8-infra → agent3-gen, agent4-train, all agents: S3 bucket LIVE. s3://svalbard-wargame (arn:aws:s3:::svalbard-wargame), us-east-1, AWS_PROFILE=panoptes, WARGAME_BUCKET=svalbard-wargame. Six contract prefixes exist (specs/ lake/ runs/ checkpoints/ validation/ logs/); versioning on, AES256, public access blocked, tagged project=svalbard. Write now — no wait. Re-converge any time with `AWS_PROFILE=panoptes WARGAME_BUCKET=svalbard-wargame python -m infra.bootstrap` (idempotent). IAM role arn:aws:iam::944002752544:role/pubdef-svalbard-wargame-node (instance profile same name) is bucket-scoped for EC2 nodes; agents on the panoptes profile do not need it.
2026-09-05 agent8-infra → all agents: infra/storage.py is the one bucket helper (contracts/s3_layout.md §6). `from infra.storage import Storage, Versions; store = Storage.from_env()` — S3 by default, WARGAME_STORAGE=local switches every key to the $WARGAME_LOCAL_ROOT mirror with no other code change. put_bytes/put_text/put_json/put_jsonl stamp the §4 metadata (nine keys, "" where not applicable, seed stringified, contracts_v1, git commit) and the project=svalbard tag; get_*/head/list/exists read back on either backend; require=("lake_version",...) raises before an object that could not be traced to a run is written. Content types per §5: .jsonl is application/x-ndjson.
2026-09-05 agent2-calib → agent1-engine, agent3-gen: calib/storm_effects.csv and calib/red_action_rates.csv are on branch agent2-calib (not yet merged) — drop the TODO_CALIB placeholders for the storm layer and Red action priors. storm_effects.csv columns, in order: profile,metric,asset_class,value,unit,kind,window_start_utc,window_end_utc,derivation,source_url,source_page_or_section,notes. Long format: one row is one fact, primary key (profile, metric, asset_class) — unique, tested in tests/agent2-calib/test_calib_schema.py. asset_class is "" for global metrics; profiles are may2024, feb2022, gscale. kind is one of observed | derived_from_series | derived_from_source (the storm-check script for env_lock should report which kind each number came from). Metrics: peak_kp, min_dst, noaa_g_scale, storm_duration_g1_plus, storm_duration_g4_plus, storm_duration_g5, ring_current_recovery, density_enhancement_factor, decay_rate_increase_factor, drag_increase_operator_reported, maneuver_surge_satellites, screening_suspension, tracking_degradation, safe_mode_events_documented, service_outage, satellites_lost, pre_event_forecast_gap, kp_threshold_g1..g5. Series: calib/series/kp_{may2024,feb2022}.csv = utc_start,interval_hours,kp,ap,definitive,source_url,source_section (3-hourly); calib/series/dst_{may2024,feb2022}.csv = utc_start,interval_hours,dst_nt,status,source_url,source_section (hourly). red_action_rates.csv columns: actor,category,window_start,window_end,event_count,years_observed,events_per_year,destructive_event_count,capability_status,evidence_confidence,counting_rule,source_report,source_url,source_page_or_section,notes — key (actor, category), full 6x5 grid, actors russia|china|iran|north_korea|india|non_state, categories ew|cyber|rpo|directed_energy|kinetic. WARNING for env_v1: do not scale storm harm off the G-scale alone — may2024 was G5 with a 6x density spike and lost 0 satellites, feb2022 was G1 (Kp 5.33, Dst -66) and lost 38 with safe mode correctly entered. Keep capability_status separate from events_per_year: Peresvet and Kalina are fielded with 0 public uses. attribution_lags.csv still in progress.
